# ============================================================================
# MasteringReady - Environment Variables / Variables de Entorno
# ============================================================================
# Copy this file to .env.local and fill in your values
# Copia este archivo a .env.local y completa tus valores
# ============================================================================

# ============================================================================
# SUPABASE CONFIGURATION / CONFIGURACIÓN DE SUPABASE
# ============================================================================
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
# Obtén estos de: https://supabase.com/dashboard/project/TU_PROYECTO/settings/api

# Public URL (safe for browser)
# URL pública (segura para navegador)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anon/Public key (safe for browser)
# Clave anónima/pública (segura para navegador)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service Role key (NEVER expose to browser - server only!)
# Clave de rol de servicio (¡NUNCA exponer al navegador - solo servidor!)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================================================
# API CONFIGURATION / CONFIGURACIÓN DE API
# ============================================================================

# Backend API URL (FastAPI on Render)
# URL de API del backend (FastAPI en Render)
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com

# ============================================================================
# STRIPE CONFIGURATION / CONFIGURACIÓN DE STRIPE
# ============================================================================
# Get these from: https://dashboard.stripe.com/apikeys
# Obtén estos de: https://dashboard.stripe.com/apikeys

# Publishable key (safe for browser)
# Clave publicable (segura para navegador)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Secret key (server only!)
# Clave secreta (¡solo servidor!)
STRIPE_SECRET_KEY=sk_test_...

# Webhook secret (for verifying Stripe webhooks)
# Secreto de webhook (para verificar webhooks de Stripe)
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Product IDs (created in Stripe Dashboard)
# IDs de productos de Stripe (creados en el Dashboard de Stripe)
# STRIPE_PRODUCT_PRO_MONTHLY=prod_xxx
# STRIPE_PRODUCT_SINGLE=prod_xxx
# STRIPE_PRODUCT_ADDON=prod_xxx

# ============================================================================
# OPTIONAL: ANALYTICS & MONITORING / OPCIONAL: ANALÍTICAS Y MONITOREO
# ============================================================================

# Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry (error tracking / seguimiento de errores)
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
SENTRY_AUTH_TOKEN=sntrys_...

# ============================================================================
# IP RATE LIMITING / LÍMITE DE TASA POR IP
# ============================================================================
# Controls anonymous user rate limiting (1 free analysis per IP)
# Controla límite de análisis para usuarios anónimos (1 gratis por IP)

# Enable IP rate limiting (disabled by default for easy launch)
# Habilitar límite por IP (deshabilitado por defecto para lanzamiento fácil)
ENABLE_IP_RATE_LIMIT=false

# Enable VPN/Proxy detection (works with IP limiting)
# Habilitar detección de VPN/Proxy (funciona con límite por IP)
ENABLE_VPN_DETECTION=true

# Optional: ipinfo.io token for better detection (free tier: 50k/month)
# Also used for GeoIP country detection for regional pricing
# Opcional: token de ipinfo.io para mejor detección (tier gratis: 50k/mes)
# También se usa para detección de país para precios regionales
# IPINFO_TOKEN=your_token_here

# ============================================================================
# REGIONAL PRICING / PRECIOS REGIONALES
# ============================================================================
# PPP-adjusted pricing is configured in the database (regional_pricing table)
# Los precios ajustados por PPP se configuran en la base de datos (tabla regional_pricing)
# See: supabase/migrations/20260125000001_pricing_restructure.sql

# ============================================================================
# DEVELOPMENT SETTINGS / CONFIGURACIÓN DE DESARROLLO
# ============================================================================

# Node environment
NODE_ENV=development

# Enable debug logging / Habilitar logs de depuración
DEBUG=false
